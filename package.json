{
  "name": "fintrack-expense-budget",
  "private": true,
  "version": "2.0.0",
  "description": "Fintrack — Smart Expense & Budget Management System built with plain HTML, CSS, JavaScript and a Python (Flask) backend.",
  "scripts": {
    "build": "node build.js",
    "build:preview": "node build.js",
    "serve": "python3 -m http.server 8000",
    "start": "python3 server.py"
  }
}
